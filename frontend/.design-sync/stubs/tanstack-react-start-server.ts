// Design-sync stub for `@tanstack/react-start/server` — see the sibling
// tanstack-react-start.ts for why these exist. Cookie/request helpers are
// server-only; in a browser preview they are inert.

export function getCookie(_name: string): string | undefined {
    return undefined;
}

export function setCookie(_name: string, _value: string, _options?: unknown): void {}

export function deleteCookie(_name: string, _options?: unknown): void {}

export function getRequestIP(_options?: unknown): string | undefined {
    return undefined;
}

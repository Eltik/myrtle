import { StatsTabSkeleton } from "frontend";

// The placeholder the profile Stats tab renders while the skins index loads:
// the same six-card grid (Collection, Elite Promotion, Class Breakdown,
// Mastery, Modules & Skins, Top Operators) in skeleton form.
export const Loading = () => <StatsTabSkeleton />;

import { EmptyState } from "frontend";

// The randomizer's pre-roll placeholder. It takes no props — the isometric dice
// stack is drawn inline as SVG using the lagoon/palm/sea-ink theme tokens.
export const AwaitingRandomizer = () => <EmptyState />;

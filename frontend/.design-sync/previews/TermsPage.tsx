import { TermsPage } from "frontend";

// The full `/terms` route: title block with effective-date badges, a numbered
// table of contents, then the nine clauses (acceptance, account security,
// acceptable use, user content, IP, termination, liability, changes, contact)
// closed by the related-documents footer. It takes no props — one story.

export const Default = () => <TermsPage />;
